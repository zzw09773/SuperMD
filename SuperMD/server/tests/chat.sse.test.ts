import { describe, it } from 'vitest';

// Lightweight smoke test placeholder for SSE endpoint.
// Phase 4 focuses on wiring the harness; assertions will be expanded in Phase 5.

describe.skip('Research SSE endpoint', () => {
  it('streams research events and terminates with [DONE]', async () => {
    // TODO: bootstrap server instance (createApp) and assert streaming contract
  });
});
